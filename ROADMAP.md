# Feature
画布页面的节点为卡片，每个卡片均由图片与文字构成。卡片分为如下几类：

Hook Card：描述一类可以稳定出量的素材的共性。
Inspiration Card：竞品，热点，话题，IP 格式的灵感。
Template Card：强参考的模板卡片，例如已经出量的 Betty IP。
Product Card：产品的截图。
Constraint Card：约束 Card，例如比例约束，风格约束等。

每个卡片之间可以链接，选择，删除等。
这个面板的操作逻辑是组合卡片作为输入，利用 gemini3pro ai 生成以创建新的卡片。